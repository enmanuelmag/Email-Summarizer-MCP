version: '3.8'

services:
  email-summarizer:
    container_name: service
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true

networks:
  mcp_network:
    name: mcp_network
