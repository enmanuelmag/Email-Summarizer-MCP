{
  "compilerOptions": {
    /* Basic Options */
    "target": "ES2020", // O "ES2020", "ESNext" - especifica el estándar de JS de salida. ES2016 es una buena base.
    "module": "ESNext", // Para Node.js, CommonJS es el estándar.
    "outDir": "./dist", // Directorio donde se guardarán los archivos .js compilados.
    "rootDir": "./src", // Directorio raíz de tus archivos .ts. Todo lo que esté aquí se compilará.
    "declaration": true,

    /* Strict Type-Checking Options */
    "strict": true, // Habilita todas las opciones de verificación estricta de tipos. Muy recomendado.
    "esModuleInterop": true, // Permite la interoperabilidad entre módulos CommonJS y ES Modules. Crucial para importar librerías.
    "skipLibCheck": true, // Omite la verificación de tipos de archivos de declaración (.d.ts) en los módulos.

    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,

    /* Module Resolution Options */
    "moduleResolution": "node", // Cómo el compilador busca módulos. "node" es el estándar para Node.js.

    /* Source Map Options */
    "sourceMap": true, // Genera archivos .map para poder depurar el código TS en el navegador o con Node.js.

    /* Advanced Options */
    "forceConsistentCasingInFileNames": true // Asegura que los nombres de archivo tengan un casing consistente.
  },
  "include": [
    "src/**/*" // Incluye todos los archivos dentro del directorio 'src' y sus subdirectorios.
  ],
  "exclude": [
    "node_modules", // Excluye la carpeta node_modules de la compilación.
    "dist" // Excluye la carpeta dist, ya que es el resultado de la compilación.
  ]
}
